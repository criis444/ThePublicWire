"use client";
import { useState } from "react";
export default function AiPage() { const [submitted, setSubmitted] = useState(false); return <main className="page-shell narrow"><p className="eyebrow">ASK THEPUBLICWIRE</p><h1 className="page-title">Get clarity. Keep the source.</h1><p className="lead">The production assistant will summarize available sources, compare reports, explain public documents, and help with writing. It will not make accusations, expose private information, or present guesses as facts.</p><form className="share-form" onSubmit={(event) => { event.preventDefault(); setSubmitted(true); }}><label>Ask about a topic<textarea placeholder="Example: Summarize the confirmed timeline for this topic." /></label><button className="primary-button">Ask</button></form>{submitted && <div className="panel"><strong>AI integration point ready.</strong><p>Connect a source-grounded retrieval system before enabling public answers. This demo deliberately does not invent an answer.</p></div>}</main>; }
